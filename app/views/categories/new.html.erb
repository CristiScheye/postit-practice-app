<%= render 'shared/content_title', title: 'New Category' %>

<%= render 'shared/errors', obj: @category %>


<br/>
<%= form_for @category do |f| %>
	<%= f.label :name, 'Category Name' %>
	<%= f.text_field :name %>
	<br/>
	<%= f.submit "Submit New Category", class: "btn btn-inverse" %>
<% end %>

